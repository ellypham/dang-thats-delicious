extends layout

block content
  .inner
    h2= title
      .map
        .autoComplete
          input.autoComplete__input(type="text" placeholder="Search for Anything")
        #map
          p Loading map...